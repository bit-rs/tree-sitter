{
  "name": "tree-sitter-bit",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for the Bit programming language",
  "main": "bindings/node",
  "scripts": {
    "build": "tree-sitter generate && node-gyp build",
    "test": "tree-sitter test"
  },
  "dependencies": {
    "nan": "^2.18.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.20.0"
  }
}